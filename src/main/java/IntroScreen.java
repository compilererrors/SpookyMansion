public class IntroScreen {
    public static void Intro() {

        String intro0 = "  .-')     _ (`-.                           .-. .-')";
        String intro1 = " ( OO ).  ( (OO  )                          \\  ( OO )";
        String intro2 = "(_)---\\_)_.`     \\ .-'),-----.  .-'),-----. ,--. ,--.   ,--.   ,--.";
        String intro3 = "/    _ |(__...--''( OO'  .-.  '( OO'  .-.  '|  .'   /    \\  `.'  /";
        String intro4 = "\\  :` `. |  /  | |/   |  | |  |/   |  | |  ||      /,  .-')     /";
        String intro5 = " '..`''.)|  |_.' |\\_) |  |\\|  |\\_) |  |\\|  ||     ' _)(OO  \\   /";
        String intro6 = ".-._)   \\|  .___.'  \\ |  | |  |  \\ |  | |  ||  .   \\   |   /  /\\_";
        String intro7 = "\\       /|  |        `'  '-'  '   `'  '-'  '|  |\\   \\  `-./  /.__)";
        String intro8 = " `-----' `--'          `-----'      `-----' `--' '--'    `--'";
        String intro9 = " _   .-')      ('-.         .-') _   .-')                              .-') _";
        String intro10 = "( '.( OO )_   ( OO ).-.    ( OO ) ) ( OO ).                           ( OO ) )";
        String intro11 = " ,--.   ,--.) / . --. /,--./ ,--,' (_)---\\_)  ,-.-')  .-'),-----. ,--./ ,--,'";
        String intro12 = " |   `.'   |  | \\-.  \\ |   \\ |  |\\ /    _ |   |  |OO)( OO'  .-.  '|   \\ |  |\\";
        String intro13 = " |         |.-'-'  |  ||    \\|  | )\\  :` `.   |  |  \\/   |  | |  ||    \\|  | )";
        String intro14 = " |  |'.'|  | \\| |_.'  ||  .     |/  '..`''.)  |  |(_/\\_) |  |\\|  ||  .     |/";
        String intro15 = " |  |   |  |  |  .-.  ||  |\\    |  .-._)   \\ ,|  |_.'  \\ |  | |  ||  |\\    |";
        String intro16 = " |  |   |  |  |  | |  ||  | \\   |  \\       /(_|  |      `'  '-'  '|  | \\   |";
        String intro17 = " `--'   `--'  `--' `--'`--'  `--'   `-----'   `--'        `-----' `--'  `--'";



        /*

        int numberofChar = intro0.length();

        for (int i = 0; i < numberofChar; i++) {
            char koko = intro0.charAt(i);
            terminal.setCursorPosition(i, 0);
            terminal.putCharacter(koko);
        }

        numberofChar = intro1.length();

        for (int i = 0; i < numberofChar; i++) {
            char koko = intro1.charAt(i);
            terminal.setCursorPosition(i, 1);
            terminal.putCharacter(koko);
        }
        numberofChar = intro2.length();

        for (int i = 0; i < numberofChar; i++) {
            char koko = intro2.charAt(i);
            terminal.setCursorPosition(i, 2);
            terminal.putCharacter(koko);
        }
        numberofChar = intro3.length();

        for (int i = 0; i < numberofChar; i++) {
            char koko = intro3.charAt(i);
            terminal.setCursorPosition(i, 3);
            terminal.putCharacter(koko);
        }
        numberofChar = intro4.length();

        for (int i = 0; i < numberofChar; i++) {
            char koko = intro4.charAt(i);
            terminal.setCursorPosition(i, 4);
            terminal.putCharacter(koko);
        }
        numberofChar = intro5.length();

        for (int i = 0; i < numberofChar; i++) {
            char koko = intro5.charAt(i);
            terminal.setCursorPosition(i, 5);
            terminal.putCharacter(koko);
        }
        numberofChar = intro6.length();

        for (int i = 0; i < numberofChar; i++) {
            char koko = intro6.charAt(i);
            terminal.setCursorPosition(i, 6);
            terminal.putCharacter(koko);
        }
        numberofChar = intro7.length();

        for (int i = 0; i < numberofChar; i++) {
            char koko = intro7.charAt(i);
            terminal.setCursorPosition(i, 7);
            terminal.putCharacter(koko);
        }

        numberofChar = intro8.length();

        for (int i = 0; i < numberofChar; i++) {
            char koko = intro8.charAt(i);
            terminal.setCursorPosition(i, 8);
            terminal.putCharacter(koko);
        }
        numberofChar = intro9.length();

        for (int i = 0; i < numberofChar; i++) {
            char koko = intro9.charAt(i);
            terminal.setCursorPosition(i, 9);
            terminal.putCharacter(koko);
        }
        numberofChar = intro10.length();

        for (int i = 0; i < numberofChar; i++) {
            char koko = intro10.charAt(i);
            terminal.setCursorPosition(i, 10);
            terminal.putCharacter(koko);
        }
        numberofChar = intro11.length();

        for (int i = 0; i < numberofChar; i++) {
            char koko = intro11.charAt(i);
            terminal.setCursorPosition(i, 11);
            terminal.putCharacter(koko);
        }
        numberofChar = intro12.length();

        for (int i = 0; i < numberofChar; i++) {
            char koko = intro12.charAt(i);
            terminal.setCursorPosition(i, 12);
            terminal.putCharacter(koko);
        }
        numberofChar = intro13.length();

        for (int i = 0; i < numberofChar; i++) {
            char koko = intro13.charAt(i);
            terminal.setCursorPosition(i, 13);
            terminal.putCharacter(koko);
        }

        numberofChar = pressplay.length();

        for (int i = 0; i < numberofChar; i++) {
            char koko = pressplay.charAt(i);
            terminal.setCursorPosition(i+18, 15);
            terminal.putCharacter(koko);
        }
        terminal.flush(); */
    }


}



